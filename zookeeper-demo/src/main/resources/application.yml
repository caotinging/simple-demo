##### spring #####
spring:
  profiles:
    active: @profiles.active@
  main:
    banner-mode: "console"
  application:
    name: zookeeper-demo
  output:
    ansi:
      enabled: always

##### log #####
logging:
  level:
    root: info
    org.springframework.web: warn
    com.hnpolice.messagepush: debug